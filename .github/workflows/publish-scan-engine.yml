name: Publish scan-engine packages

on:
  workflow_dispatch: {}
  release:
    types: [published]

permissions:
  contents: read
  id-token: write

jobs:
  publish:
    runs-on: ubuntu-latest
    environment: npm
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          registry-url: https://registry.npmjs.org

      - name: Ensure npm supports trusted publishing
        run: npm install -g npm@^11.5.1

      - name: Install root dependencies
        run: npm ci
        working-directory: ${{ github.workspace }}

      - name: Resolve version from tag
        id: version
        run: |
          VERSION="${GITHUB_REF_NAME#v}"
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"

      - name: Build scan-engine
        run: npm run build
        working-directory: packages/switch-scanner-engine

      - name: Prepare scan-engine version
        run: npm version "${{ steps.version.outputs.version }}" --no-git-tag-version
        working-directory: packages/switch-scanner-engine

      - name: Publish scan-engine
        run: npm publish --access public
        working-directory: packages/switch-scanner-engine

      - name: Build scan-engine-dom
        run: npm run build
        working-directory: packages/scan-engine-dom

      - name: Prepare scan-engine-dom version
        run: |
          npm version "${{ steps.version.outputs.version }}" --no-git-tag-version
          npm pkg set dependencies.scan-engine="^${{ steps.version.outputs.version }}"
        working-directory: packages/scan-engine-dom

      - name: Publish scan-engine-dom
        run: npm publish --access public
        working-directory: packages/scan-engine-dom

      - name: Build react-scan-engine
        run: npm run build
        working-directory: packages/react-scan-engine

      - name: Prepare react-scan-engine version
        run: |
          npm version "${{ steps.version.outputs.version }}" --no-git-tag-version
          npm pkg set dependencies.scan-engine="^${{ steps.version.outputs.version }}"
        working-directory: packages/react-scan-engine

      - name: Publish react-scan-engine
        run: npm publish --access public
        working-directory: packages/react-scan-engine
