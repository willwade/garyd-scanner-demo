---
layout: ../layouts/BookLayout.astro
title: "Chapter 22: Implementing Scanners"
prevHref: "/book/chapter-21"
prevLabel: "Chapter 21: User Profiles"
nextHref: "/book/chapter-23"
nextLabel: "Chapter 23: Accessibility Testing"
---

# Chapter 22: Implementing Scanners

For developers: keep scanning logic deterministic, configuration-driven, and measurable. OTs need predictable behavior that they can tune; developers need clear parameters with measurable effects.

## Why This Matters

Single-switch scanning performance is dominated by timing, layout, and errors. If the implementation hides or blends those factors, clinicians and developers can’t reliably improve text entry rate (TER). The goal here is a predictable system with explicit settings and instrumentation.

## Settings Optimization (Koester & Simpson 2014)

A structured decision tree improves text entry rate (TER). In seminal clinical research by Heidi Koester and Richard Simpson (2014, *Journal of Rehabilitation Research and Development*), systematically tuning scanning parameters using evidence-based heuristics produced an average **120% increase in Text Entry Rate (TER)** (from a baseline of 1.42 WPM up to 6.51 WPM).

A foundational rule of this clinical model is the **25% scanning error threshold**: if errors exceed 25% of selections, you **must** focus on reducing errors before optimizing speed or layout.

<div class="info-box">
  💡 <strong>In Plain English (Why Fix Errors First?):</strong><br />
  If a client makes mistakes more than 1 out of 4 times (>25%), pushing them to scan faster actually makes them <em>slower</em>! Every time they miss a target, they must press Backspace, wait for the scan to cycle around again, and re-select. That error repair penalty destroys their typing speed.<br /><br />
  By first slowing down the scan rate or adding a 1st-item pause, their error rate drops below 25%. Once errors are under control, their net communication speed automatically doubles!
</div>

<div class="figure-card">
  <img src={`${import.meta.env.BASE_URL}figures/koester-simpson-2014-flowchart.svg`} alt="Koester and Simpson 2014 settings optimization flowchart" style="width: 100%; height: auto; display: block;" />
  <div class="figure-caption">Decision tree for identifying revised scanning settings (Koester & Simpson, 2014).</div>
</div>

<div class="highlight-box">
  🩺 <strong>Clinical Rule:</strong> Fix switch activation consistency → Measure scanning error rate → If &gt;25%, adjust timing parameters (scan delay, 1st-item delay, acceptance delay); If &le;25%, focus on dead time reduction, word prediction tuning, and layout frequency.
</div>

## What You Need to Measure

At minimum, capture three quantitative inputs before attempting to optimize scanning settings:

- **Switch press time** (mean reaction time `RT` and standard deviation `SD`). Use this with the .65 rule (`T_scan = RT ÷ 0.65`) to set a starting scan delay (see Chapter 13).
- **Scanning error rate (%)**: `Error Rate = (Incorrect Selections + Timing Errors) ÷ Total Selections × 100`.
- **Text Entry Rate (TER)**: Measured in words per minute (`WPM = (Characters ÷ 5) ÷ Minutes`).

If you log individual scan step telemetry, you can diagnose whether errors are caused by a **late first press** (scan rate too fast), a **late next press** (inadequate 1st-item delay), **unintentional presses** (insufficient acceptance delay/debounce), or **missed first rows** (manual vs auto initiation issue).

## Decision Branch Summary

Use this as a short-form implementation guide for clinicians and assistive technology developers:

- If **Switch Performance is NOT OK**: revise switch mounting, switch type, or acceptance delay; re-test switch activation consistency before tuning software scanning.
- If **Performance is OK**: run a standardized scan test to compute error rate and baseline TER.
- If **TER cannot improve**: maintain current profile.
- If **Errors &gt; 25%**: apply error-reduction interventions (slower scan delay, 1st-item pause, acceptance delay).
- If **Errors &le; 25%**: apply efficiency improvements (reduce dead time, optimize word prediction list length, switch to frequency-based ETAOIN SHRDLU layout).

## Error-Reduction Interventions

When error rate exceeds 25%, target the specific failure mode:

- **First press too late** → increase scan delay by 15–25% (or set to `RT ÷ 0.65`) and add a 500–1000 ms scan-initiation delay.
- **Next press too late** → add a 250–500 ms 1st-item delay on rows and columns.
- **Unintentional presses / tremors** → increase acceptance delay (+150–300 ms hold window) and enable debounce (50–100 ms).
- **Missing first-row selections** → switch to manual scan initiation or extend 1st-row pause.

These are intentionally conservative changes; once error rate drops below 25%, move to efficiency improvements.

## Efficiency Improvements

Once errors are controlled (Error Rate ≤ 25%), aim for fewer scan steps and less dead time:

- Remove unnecessary scan targets (title bar, window control items).
- Put letters + prediction candidates first; stay in prediction mode until word completion.
- Use a frequency-based letter layout (ETAOIN SHRDLU) unless the user specifically requires an alphabetical board.
- Tune word prediction list size (3–6 candidates); excessively long prediction lists add visual search overhead that negates keystroke savings.

## Implementation Checklist for Developers

- Settings are stored as explicit typed fields and versioned per user profile.
- All timing parameters are displayed in milliseconds (ms) in the UI.
- Each selection event logs: target ID, selected ID, scan delay, step count, and error type.
- The system can replay a scanning session to diagnose error clusters.
- A standardized scan-test mode exists for short, repeatable measurements.

## Settings Advisor (Koester & Simpson Clinical Algorithm)

<div class="wizard-container" id="advisorWizard">
  <div class="wizard-tabs">
    <button class="wizard-tab-btn active" id="advisorTabStep1" type="button">
      <span>1️⃣ Step 1: Telemetry & Error Rate</span>
    </button>
    <button class="wizard-tab-btn" id="advisorTabStep2" type="button">
      <span>2️⃣ Step 2: Observed Failure Modes</span>
    </button>
    <button class="wizard-tab-btn" id="advisorTabStep3" type="button">
      <span>3️⃣ Step 3: Clinical Prescriptions</span>
    </button>
  </div>

  <div class="wizard-step-pane active" id="advisorPaneStep1">
    <div class="info-box" style="margin-top: 0; background-color: #eff6ff; border-left-color: #3b82f6; color: #1e3a8a;">
      <strong>Step 1: Enter Telemetry Parameters:</strong> Input client error rate, current scan delay, and reaction time below to evaluate against the Koester & Simpson 25% Error Threshold model.
    </div>

    <div class="reaction-test" id="settingsAdvisor" style="box-shadow: none; border: none; padding: 0;">
      <div style="display: grid; grid-template-columns: repeat(auto-fit, minmax(220px, 1fr)); gap: 16px; background: #f8fafc; padding: 20px; border-radius: 14px; border: 1px solid #cbd5e1;">
        <label>
          <strong style="font-size: 1rem;">Switch Performance OK?</strong><br />
          <select id="advisorPerformance" style="width: 100%; padding: 10px; margin-top: 6px; font-size: 1rem; border-radius: 8px; border: 1px solid #cbd5e1;">
            <option value="yes">Yes (Consistent physical trigger)</option>
            <option value="no">No (Inconsistent physical trigger)</option>
          </select>
        </label>
        <label>
          <strong style="font-size: 1rem;">TER / Errors can improve?</strong><br />
          <select id="advisorImprove" style="width: 100%; padding: 10px; margin-top: 6px; font-size: 1rem; border-radius: 8px; border: 1px solid #cbd5e1;">
            <option value="yes">Yes</option>
            <option value="no">No</option>
          </select>
        </label>
        <label>
          <strong style="font-size: 1rem;">Scanning Errors (%)</strong><br />
          <input id="advisorErrors" type="number" min="0" max="200" value="30" style="width: 100%; padding: 10px; margin-top: 6px; font-size: 1rem; border-radius: 8px; border: 1px solid #cbd5e1;" />
        </label>
        <label>
          <strong style="font-size: 1rem;">Current Scan Delay (ms)</strong><br />
          <input id="advisorCurrentDelay" type="number" min="100" max="5000" value="1000" style="width: 100%; padding: 10px; margin-top: 6px; font-size: 1rem; border-radius: 8px; border: 1px solid #cbd5e1;" />
        </label>
        <label>
          <strong style="font-size: 1rem;">Mean Reaction Time (ms)</strong><br />
          <input id="advisorMeanRt" type="number" min="0" max="3000" value="650" style="width: 100%; padding: 10px; margin-top: 6px; font-size: 1rem; border-radius: 8px; border: 1px solid #cbd5e1;" />
        </label>
      </div>

      <div style="margin-top: 20px; text-align: right;">
        <button class="primary" id="btnAdvisorToStep2" type="button" style="padding: 12px 24px; font-size: 1.05rem;">
          Next: Failure Modes ➔
        </button>
      </div>
    </div>
  </div>

  <div class="wizard-step-pane" id="advisorPaneStep2">
    <div class="info-box" style="margin-top: 0; background-color: #eff6ff; border-left-color: #3b82f6; color: #1e3a8a;">
      <strong>Step 2: Select Observed Failure Modes:</strong> Check all behavior patterns observed during scanning sessions to pinpoint root causes.
    </div>

    <div style="display: grid; grid-template-columns: repeat(auto-fit, minmax(260px, 1fr)); gap: 14px; background: #f8fafc; padding: 22px; border-radius: 14px; border: 1px solid #cbd5e1; font-size: 1.02rem;">
      <label style="cursor: pointer; display: flex; align-items: center; gap: 8px;"><input type="checkbox" id="advisorFirstLate" checked style="width: 18px; height: 18px;" /> <strong>First press too late</strong> (Scan rate too fast)</label>
      <label style="cursor: pointer; display: flex; align-items: center; gap: 8px;"><input type="checkbox" id="advisorNextLate" style="width: 18px; height: 18px;" /> <strong>Next press too late</strong> (Row entry delay)</label>
      <label style="cursor: pointer; display: flex; align-items: center; gap: 8px;"><input type="checkbox" id="advisorUnintentional" style="width: 18px; height: 18px;" /> <strong>Unintentional presses</strong> (Tremor/bounce)</label>
      <label style="cursor: pointer; display: flex; align-items: center; gap: 8px;"><input type="checkbox" id="advisorMissingRow" style="width: 18px; height: 18px;" /> <strong>Missing first-row selections</strong></label>
      <label style="cursor: pointer; display: flex; align-items: center; gap: 8px;"><input type="checkbox" id="advisorDeadTime" style="width: 18px; height: 18px;" /> <strong>Too much dead time</strong> (Scan rate too slow)</label>
      <label style="cursor: pointer; display: flex; align-items: center; gap: 8px;"><input type="checkbox" id="advisorWordPred" style="width: 18px; height: 18px;" /> <strong>Inefficient word prediction</strong></label>
      <label style="cursor: pointer; display: flex; align-items: center; gap: 8px;"><input type="checkbox" id="advisorLayout" style="width: 18px; height: 18px;" /> <strong>Inefficient layout / pattern</strong></label>
    </div>

    <div class="button-row" style="margin-top: 20px; justify-content: space-between;">
      <button class="secondary" id="btnAdvisorBackToStep1" type="button" style="padding: 10px 20px;">
        ← Back to Step 1
      </button>
      <button class="primary" id="advisorRun" type="button" style="padding: 12px 24px; font-size: 1.05rem;">
        Generate Clinical Recommendations ➔
      </button>
    </div>
  </div>

  <div class="wizard-step-pane" id="advisorPaneStep3">
    <div style="background: #eef5fb; padding: 22px; border-radius: 14px; border: 1.5px solid var(--accent);">
      <h3 style="margin-top: 0; color: var(--accent);">Clinical Diagnosis</h3>
      <p style="font-size: 1.1rem; font-weight: 700;"><span id="advisorSummary">Click "Generate Clinical Recommendations" on Step 2 to generate diagnostic results.</span></p>
      <div id="advisorResults" style="margin-top: 16px;"></div>
    </div>

    <div style="margin-top: 20px; text-align: left;">
      <button class="secondary" id="btnAdvisorBackToStep2" type="button" style="padding: 10px 20px;">
        ← Back to Step 2: Failure Modes
      </button>
    </div>
  </div>
</div>
